import React from 'react'

const ReduxHome = () => (
<div>
  <h1>Welcome to the Simple Redux Route Website!</h1>
  <p>In this demo we will display a list of shows from the tvMaze website using their API!</p>
  <p>Click on the Show link above to fetch a list of shows with the query as THRONE from TVMAZE website. We will be using Redux to fetch the shows.</p>
</div>
)

export default ReduxHome
